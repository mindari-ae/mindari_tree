name: example
description: An example for the mindari_tree package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  mindari_tree:
    path: ../

  url_launcher: ^6.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true